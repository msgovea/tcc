<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="AppTheme2" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="SplashTheme">
        <!-- Customize your theme here. -->
        <item name="android:actionBarStyle">@style/Theme.AppCompat.NoActionBar</item>
        <item name="android:windowActionBarOverlay">true</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="StyledTilEditTextTheme">
        <item name="colorControlNormal">#60A9A9A9</item>
        <item name="colorControlActivated">#60A9A9A9</item>
    </style>

    <style name="StyledTilEditText">
        <item name="android:theme">@style/StyledTilEditTextTheme</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:textColorHint">#60A9A9A9</item>
    </style>

    <style name="meuTema">
        <item name="colorControlNormal">#FFFF</item>
        <item name="colorControlActivated">#FFFF</item>
        <item name="android:textColor">#FFFF</item>
        <item name="android:textColorHint">#FFFF</item>
    </style>

    <style name="EmptyList">
        <item name="android:textColor">@color/primary_text</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_marginLeft">16dp</item>
        <item name="android:layout_marginRight">16dp</item>
    </style>

    <style name="WordPress.EmptyList.Title" parent="EmptyList">
        <item name="android:textSize">24dp</item>
        <item name="android:fontFamily">sans-serif-light</item>
    </style>

    <style name="WordPress.EmptyList.Description" parent="EmptyList">
        <item name="android:textSize">24dp</item>
    </style>

</resources>
